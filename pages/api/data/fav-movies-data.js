export const Movies = [
  {
    id: 1,
    title: "title",
    images: Array.apply("title", 4).map(el, i => el = el + i + ".svg")
  },
  {
    id: 1,
    title: "title",
    images: Array.apply("title", 4).map(el, i => el = el + i + ".svg")
  },
  {
    id: 1,
    title: "title",
    images: Array.apply("title", 4).map(el, i => el = el + i + ".svg")
  },
  {
    id: 1,
    title: "title",
    images: Array.apply("title", 4).map(el, i => el = el + i + ".svg")
  },
  {
    id: 1,
    title: "title",
    images: Array.apply("title", 4).map(el, i => el = el + i + ".svg")
  },
  {
    id: 1,
    title: "title",
    images: Array.apply("title", 4).map(el, i => el = el + i + ".svg")
  },
  {
    id: 1,
    title: "title",
    images: Array.apply("title", 4).map(el, i => el = el + i + ".svg")
  },
  {
    id: 1,
    title: "title",
    images: Array.apply("title", 4).map(el, i => el = el + i + ".svg")
  },
  {
    id: 1,
    title: "title",
    images: Array.apply("title", 4).map(el, i => el = el + i + ".svg")
  },
]